# Which screens to photograph, and in which appearance.
#
# Edit this file and commit — the Preview screens workflow runs on the change and
# posts the shots into its job log (and as artifacts). GitHub's web editor works
# fine from a phone.
#
# screens:    studio, home, archive, course, summit, yearbook, prints, wall-art,
#             map-studio, gallery-studio, detail
#             wall-art accepts a style: wall-art:ridgeline, wall-art:rings,
#             wall-art:pulse, wall-art:bloom, wall-art:constellation, wall-art:grid
#             print-engine runs the print writer's self-check and reports pass/fail
# appearance: light, dark, or both
#
# Requested for b406: the print engine's self-check — does the hand-written PNG decode,
# do its scanlines run the right way, and does a 7200x10800 sheet actually get written on
# a phone-class device. Plus the storefront, where the Photo Wall tile now shows the wall
# inside its frame.

screens=print-engine,map-studio
appearance=light
